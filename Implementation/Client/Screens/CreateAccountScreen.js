import React from 'react'
import { View, StyleSheet, Text } from 'react-native'

const CreateAccountScreen = () => {
    return(
        <Text>Create Acc Screen</Text>
    )
}

const styles = StyleSheet.create({})

export default CreateAccountScreen;